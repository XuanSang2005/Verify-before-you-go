import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function SharePreviewPlaceholder() {
  return (
    <PlaceholderScreen
      checkpoint="CP12"
      description="Privacy-safe sharing preview will be implemented only when CP12 is approved."
      title="Share observations"
    />
  );
}
